<h1>Users#signin</h1>
<p>Find me in app/views/users/signin.html.erb</p>

<%= form_for :user, :url => check_signin_path  do |f| %>

	<%= f.label :email %>
	<%= f.text_field :email %>

	<%= f.label :password %>
	<%= f.password_field :password %>

	<%= f.submit %>
<% end %>