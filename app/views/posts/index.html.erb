<h3>Recent posts: </h3>

<% if @posts.count > 0 %>
  <%= render "layouts/relevant_posts" %>
<% else %>
<h4>There are no posts yet!</h4>
    <% if signed_in? %>
        <%= link_to "Add one", :newpost %>
    <% end %>
    
<% end %>